apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: gatekeeper
  namespace: flux-system
spec:
  interval: 10m
  path: ./k8s/gatekeeper
  prune: true
  sourceRef:
    kind: GitRepository
    name: user-app-repo
  dependsOn:
    - name: infrastructure
