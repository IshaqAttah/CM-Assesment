apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: service-mesh
  namespace: flux-system
spec:
  interval: 10m
  path: ./service-mesh
  prune: true
  sourceRef:
    kind: GitRepository
    name: user-app-repo
  dependsOn:
    - name: infrastructure
